const getFP = (): string => {
  return 'fingerprint';
};

export const checkLoginStatus = () => {
  // check local storage
};

export function checkWebAuthn(res: boolean): boolean {
  return res;
}

export function checkWebCrypto(res: boolean): boolean {
  return res;
}

export function checkWeb3(res: boolean): boolean {
  return res;
}
